{
  "name": "multicb",
  "version": "1.2.2",
  "description": "Simple way to aggregate multiple node-style callbacks",
  "repository": "pfrazee/multicb",
  "dependencies": {},
  "devDependencies": {
    "interleavings": "~0.3.0"
  },
  "scripts": {
    "test": "set -e; for t in test/*.js; do node $t; done"
  }
}
